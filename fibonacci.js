console.log("test")



/*function fibonacci(n1, n2){

for(i=0; i<=100; i++){
    console.log(n1+n2)
    var temp = n1;
    n1 = n2;
    n2 = temp + n2;
    } 
}
*/
/*
function fibonacci () {
    var max = prompt("enter limit");
    var series = [0, 1];
    console.log(0);
    console.log(1);
    var count = 2; // because the first 2 terms are already in the series
    while (count < max){
        series[series.length] = series[series.length - 1] + series[series.length - 2];
        console.log(series[count]);
        count++;
    }
}
*/

//triangle creator

var base = prompt("Please enter the size of the base of the triangle")
var spaces = base/2
for(i=0; i<100; i++)
{
    for(o=spaces; o< ){
        console.log(spaces)
    spaces = spaces - 1;
    }
    
}
    


















